package ParseGroup;
/*
public class VerbParser extends Parser {

//    protected String
    private String[] verbAry = {
            "take", "get", "pick up", "pickup"
    };

    public VerbParser () {

    }

    public boolean parse (String text) {

        int prevTextPtr = getParserPtr();
        eatBlanks ();

        for (int i=0; i!=verbAry.length; i++) {
            if (eatSubString(verbAry[i])) {
                verb = verbAry[i];
                return true;
            }
        }

        setParserPtr(prevTextPtr);
        return false;
    }


}
*/

